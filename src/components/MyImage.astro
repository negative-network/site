---
import { Image } from 'astro:assets';

const { imageSrc, imageAlt, imageClasses } = Astro.props;

const images = import.meta.glob("@assets/images/**/*");

---

{images[imageSrc] instanceof Function ? <Image src={images[imageSrc]()} alt={imageAlt} class:list={imageClasses}/> : null}

